{
  "properties": [
    "msg.sender == admin",
    "msg.sender == pendingAdmin && msg.sender != address(0)",
    "msg.sender == admin || msg.sender == whitelistGuardian"
  ],
  "mode": "assertion",
  "optimizer": {
    "enabled": true,
    "runs": 200
  }
}